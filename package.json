{
  "name": "pqueue-sample",
  "version": "1.0.0",
  "description": "Sample project for p-queue",
  "main": "index.js",
  "scripts": {
    "build": "tsc --noEmit && rm -rf dist && node build.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Aminevsky",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^2.4.1"
  },
  "dependencies": {
    "@types/node": "~14.17.0",
    "axios": "^0.22.0",
    "esbuild": "^0.13.4",
    "p-queue": "^7.1.0",
    "typescript": "^4.4.3"
  }
}
